<UserControl x:Class="SignalGeneratorApp.Views.CustomTooltipView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF" 
             xmlns:models="clr-namespace:SignalGeneratorApp.Models" d:DataContext="{d:DesignInstance Type=models:TooltipData}"
             mc:Ignorable="d"
             d:DesignHeight="100" d:DesignWidth="200"
             Background="Navy" BorderBrush="LightBlue" BorderThickness="1"  Padding="5">
    <StackPanel Orientation="Vertical">
        <TextBlock x:Name="TimeTextBlock" Foreground="White" FontSize="12" Margin="0,2"/>
        <TextBlock x:Name="ValueTextBlock" Foreground="White" FontSize="12" Margin="0,2"/>
        <!-- Опционально: можно добавить отображение типа сигнала или других мета-данных,
             если они доступны через контекст тултипа -->
    </StackPanel>
</UserControl>
